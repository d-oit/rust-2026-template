name: Update Architecture Diagram

on:
  push:
    paths:
      - "Cargo.toml"
      - "crates/**/Cargo.toml"
      - ".agents/skills/*/SKILL.md"
      - "docs/diagram-config.json"
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Graphviz
        run: sudo apt-get install -y graphviz

      - name: Install mdbook
        run: |
          curl -sSL https://github.com/rust-lang/mdBook/releases/download/v0.4.40/mdbook-v0.4.40-x86_64-unknown-linux-gnu.tar.gz | tar xz -C /usr/local/bin

      - name: Regenerate diagram
        run: |
          python .agents/skills/architecture-diagram/scripts/generate_diagram.py \
            --root . \
            --out .template/architecture.svg
          cp .template/architecture.svg docs/src/architecture.svg

      - name: Build mdbook
        run: mdbook build docs

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .template/architecture.svg docs/src/architecture.svg docs/book/
          git commit -m "docs: regenerate architecture diagram and rebuild mdbook [skip ci]"
          git push
