---
name: Deploy mdBook to GitHub Pages

on:
  push:
    branches: ["main"]
    paths:
      - "docs/**"
      - ".template/overview.svg"
      - ".github/workflows/deploy-docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Install mdbook
        run: |
          MDBOOK_URL="https://github.com/rust-lang/mdBook/releases/download/v0.4.40"
          curl -sSL "${MDBOOK_URL}/mdbook-v0.4.40-x86_64-unknown-linux-gnu.tar.gz" | tar xz
          sudo mv mdbook /usr/local/bin/

      - name: Sync overview SVG
        run: bash scripts/sync-architecture.sh

      - name: Setup mdbook
        run: bash scripts/setup-mdbook.sh

      - name: Build mdbook
        run: mdbook build docs

      - name: Upload artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9  # v5.0.0
        with:
          path: docs/book

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128  # v5.0.0
